2. The Skill Creator Worker (builder-agents.md)
This is the "Coder" of the agents. Its only job is to output perfectly formatted .md files that follow your "Munch Formula."

Role: Following the SKILL.md guide you provided, it writes the metadata, the tool tables, and the "Always" protocols.

Validation: It doesn't just write the file; it performs a "Smoke Test" by trying to use one of the tools it just defined to ensure the syntax is correct.