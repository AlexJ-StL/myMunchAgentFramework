# The Visual Workflow (Diagram Plan)

For those "bonus points," the README needs a diagram. It should look like this:

[User Intent] → [Director Agent]
↓
[jMRI Discovery] (Scan for available repos/docs)
↓
[Decomposition] (Break into IDs: api.ts::Endpoint#const)
↓
[Worker Agent] (Uses skill-jcodemunch.md to fetch/edit ONLY that ID)
↓
[Final Output + Token ROI Report]